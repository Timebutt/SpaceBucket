(function () {
    angular
        .module('app')
        .controller('PowerController', [
			'$http', '$scope',
            PowerController
        ]);	
		
    function PowerController($http, $scope) {
        var vm = this;       

    }
})();
